# Profiles

Замеры по шагам оптимизации:

- `00_baseline` - первая рабочая версия с ручной сортировкой.
- `01_sort` - замена ручной сортировки на `sort.Float64s`.
- `02_parallel` - расчет суммы, минимума и максимума по чанкам.
- `03_final` - порог для parallel path и quickselect для `p95`.
